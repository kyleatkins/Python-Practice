#!usr/env/python
#Modeling a soccer team with substitutions

#Teams stored in dictionary {Name:TotalPlayers}
teams = {}
#Players stored in dictionary {Name:Numbers}
players = {}
# Player positions stored in a list
forward = []
midfield = []
defense = []
goalie = []

class Global(object):
    def __init__(self, name):
        self.name = name
                print "Teams currently in leage: %s" % dict_teams

        def createTeam(self, name)
                name = self.name

        #creates a player list with names, numbers, and preferred positions
    def Player(self, name, number, position):
        self.name = name
        self.number = number
        self.position = position
:
